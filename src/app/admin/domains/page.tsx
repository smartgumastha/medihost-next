export const metadata = { title: 'Domains — Admin' };

export default function DomainsPage() {
  return (
    <div className="bg-white border border-gray-200 rounded-xl p-8 text-center">
      <div className="text-4xl mb-4">🔗</div>
      <h2 className="text-xl font-bold text-gray-900">Domain Management</h2>
      <p className="text-gray-500 mt-2 max-w-md mx-auto">Track all registered domains, DNS status, renewals. Feature being built.</p>
    </div>
  );
}
