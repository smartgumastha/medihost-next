import { ApprovedLabs } from '@/components/admin/approved-labs';

export const metadata = { title: 'Approved Labs — Admin' };

export default function ApprovedLabsPage() {
  return <ApprovedLabs />;
}
