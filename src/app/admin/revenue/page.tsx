import { PaymentsDashboard } from '@/components/admin/payments-dashboard';

export const metadata = { title: 'Payments & Revenue — Admin' };

export default function RevenuePage() {
  return <PaymentsDashboard />;
}
