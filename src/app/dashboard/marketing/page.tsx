import { OnboardingWizard } from "@/components/dashboard/marketing/onboarding-wizard";

export const metadata = { title: "Marketing Setup — MediHost" };

export default function MarketingPage() {
  return <OnboardingWizard />;
}
