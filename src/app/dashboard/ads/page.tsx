import { AdsManager } from "@/components/dashboard/marketing/ads-manager";

export const metadata = { title: "Ads Manager \u2014 MediHost" };

export default function AdsPage() {
  return <AdsManager />;
}
