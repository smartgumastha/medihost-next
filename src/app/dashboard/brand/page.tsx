import { BrandStudio } from "@/components/dashboard/marketing/brand-studio";

export const metadata = { title: "Brand Studio \u2014 MediHost" };

export default function BrandPage() {
  return <BrandStudio />;
}
