import { JoinPage } from '@/components/hemato/join-page';

export const metadata = {
  title: 'Join Hemato as a Partner',
  description: 'Partner with Hemato — Register your diagnostic lab, clinic, pharmacy, or join as a phlebotomist.',
};

export default function Join() {
  return <JoinPage />;
}
